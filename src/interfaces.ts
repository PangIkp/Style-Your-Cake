export interface ToppingItem {
    name: string;
    quantity: number;
}

export interface ProductItem {
    id: string;
    productName: string;
    price: number;
    category: string;
    productPic: string;
}